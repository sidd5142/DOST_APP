<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DOST" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/light_blue_A200</item> -->
        <item name="colorPrimaryDark">#6fbbd3</item>
    </style>

    <style name="Theme.DOST" parent="Base.Theme.DOST" />

    <style name="Theme.DOST.Fullscreen" parent="Theme.DOST">
        <item name="android:actionBarStyle">@style/Widget.Theme.DOST.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.DOST.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>